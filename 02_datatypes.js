console.log("Hello")

let name = "Arya"; //This is string variable.
let age = 18; //This is number variable
let isLoggedIn = true; //This is boolean variable.

//Primitive variables

    //number -> 2^53.
    //strings -> use '' or ""(prefferred).
    //boolean -> true or false;
    // null -> standalone value(disccussed later)(it's a objesct not a type)
    // undefined -> not defining a value to the variable(type is assigned)
    //symbols => unique.
